import { CourseDateDirective } from './course.directive';

describe('CourseDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseDateDirective();
    expect(directive).toBeTruthy();
  });
});
