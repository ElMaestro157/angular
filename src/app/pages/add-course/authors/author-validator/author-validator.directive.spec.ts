import { AuthorValidatorDirective } from './author-validator.directive';

describe('AuthorValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
